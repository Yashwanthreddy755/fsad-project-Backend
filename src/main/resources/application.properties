spring.application.name=scheduling
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/fsadproject
# Assuming root:root which is common for learning setups, configurable as needed
spring.datasource.username=root
spring.datasource.password=yashureddy09
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Frontend URL for password reset links and CORS.
# Set APP_FRONTEND_BASE_URL to your deployed frontend URL, for example:
# APP_FRONTEND_BASE_URL=https://your-app.vercel.app
app.frontend.base-url=${APP_FRONTEND_BASE_URL:http://localhost:5173}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:${app.frontend.base-url}}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=venkatshivanuvvula@gmail.com
spring.mail.password=ymwn ikoz sduc cncy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
